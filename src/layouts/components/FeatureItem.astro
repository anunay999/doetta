---
const { emoji, text, highlight } = Astro.props;
const parts = text.split(highlight);
---

  <div class="flex items-center space-x-4 m-2 container xl">
    <span class="text-3xl">{emoji}</span>
    <p class="text-gray-800">
      {parts[0]}
      <span class="bg-gradient-to-r from-pink-400 to-orange-300 text-white px-2 py-1 rounded font-bold">
        {highlight}
    </span>
      {" "+ parts[1]}
    </p>
  </div>

